from django.urls import path
from . import views
from .views import *

urlpatterns = [
    # path('certificates/new', CertCreate.as_view(), name='get_new_certificate'),
    # path('certificates/new', get_new_certificate, name='get_new_certificate'),
    # path('certificates/done', certificate_done, name='certificate_done'),
    # path('certificates/', get_certificates, name='certificates'),
]